# Development environment for dcTMD.
#   conda env create -f environment.yml
#   conda activate dcTMD
name: dcTMD
channels:
  - conda-forge
dependencies:
  - python>=3.9
  - numpy>=1.21.0
  - scipy
  - scikit-learn
  - beartype>=0.10.4
  - tqdm
  - click>=7.0.0
  - matplotlib>=3.7
  - pip